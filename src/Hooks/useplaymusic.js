import { useEffect, useRef, useState } from "react";


const usePlayMusic = (url, duration, setDuration, currentTime, setCurrentTime)=>{

    const refaudio = useRef(null);


   
}

export default usePlayMusic;
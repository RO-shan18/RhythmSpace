import { useParams } from "react-router-dom";

const SearchAudiobookdetail = ()=>{

    const {audiobookid} = useParams();

    return (
        <div></div>
    )
}

export default SearchAudiobookdetail;